# Azure service principal / Entra ID app registration credentials generated
# by infrastructure/bootstrap.sh. Kept locally (and reused across
# re-deploys / read by infrastructure/teardown.sh) but must never be
# committed, since each contains a client secret.
infrastructure/azure-credentials.json
infrastructure/backstage-credentials.json
.vscode
