# Copilot instructions for azure-crossplane-demo

Workshop/demo material: a GitOps setup that bootstraps a local Kubernetes
cluster (kiac) and installs Crossplane with an Azure provider via Flux. There
is no application code, build step, or test suite — the repo is entirely
declarative manifests plus two setup shell scripts.

## Validating changes

There's no build/test. Validation is via `pre-commit` (pre-commit-hooks,
yamllint, zizmor, gitleaks):

```sh
pre-commit run --all-files          # everything
pre-commit run --files <path>       # a single file (the closest thing to "run one test")
bash -n infrastructure/bootstrap.sh # shell syntax check for a single script
```

Hooks enforce: valid YAML/JSON, no private keys/secrets (gitleaks), LF line
endings, executable shell scripts with shebangs, and pretty-printed JSON.
Run the full `pre-commit` suite (or at least the relevant hook) before
considering a change to manifests or scripts done.

All YAML files must begin with the YAML document marker `---` on the first
line, including single-document files and Kustomize manifests.

## Architecture: Flux reconciliation chain

`clusters/dev/` is the single path Flux watches (see
`clusters/dev/flux-system/gotk-sync.yaml`, generated by `flux bootstrap` —
**don't hand-edit `gotk-components.yaml`/`gotk-sync.yaml`**, they're
regenerated). Every directory that's a Flux `Kustomization` `path:` target
has its **own explicit `kustomization.yaml`** — this is load-bearing, not
incidental (see "CRD-dependency deadlock" below). Understanding changes here
requires following the chain across several files:

1. `clusters/dev/kustomization.yaml` — the root, applied by the `flux-system`
   Kustomization: references `flux-system/`, `crossplane/`, and the `teams`
   pointer Kustomization (below).
2. `clusters/dev/crossplane/` — cluster-specific Crossplane resources grouped
  by responsibility. `core/` contains the namespace and Helm installation;
  `flux-kustomizations/` contains the pointer Kustomization CRs that enforce
  apply order. The provider, provider config, and functions directories are
  deliberately not referenced by the parent Kustomization.
3. `clusters/dev/crossplane/providers/azure/provider.yaml` — the `Provider`
   package CR. Applied by the dependent `crossplane-azure-provider`
   Kustomization (`dependsOn: flux-system`, its own `retryInterval`/`timeout`)
   because `providers.pkg.crossplane.io` only exists once Crossplane core
   (installed by the `HelmRelease` above) has finished installing — it can't
   be applied in the same batch as the `HelmRelease`.
4. `clusters/dev/crossplane/provider-configs/azure/provider-config.yaml` — the
   `ProviderConfig`, applied by the dependent `crossplane-azure-config`
   Kustomization (`dependsOn: crossplane-azure-provider`) because its CRD
   only exists once the `Provider` package above has installed. It reads
   Azure credentials from a `Secret` named `azure-secret` (key `creds`) in
   `crossplane-system` — **not** part of any manifest in git; it's created
   directly in-cluster by `infrastructure/bootstrap.sh` (see below).
5. `clusters/dev/crossplane/functions/` — Composition `Function` packages
   (`function-patch-and-transform`, `function-go-templating`,
   `function-kro`), applied by the dependent `crossplane-functions`
   Kustomization (`dependsOn: flux-system`) because
   `functions.pkg.crossplane.io` only exists once Crossplane core has
   installed.
6. `compositions/` — shared Crossplane `CompositeResourceDefinition`s and
  `Composition`s, grouped first by cloud provider and then by composition
  (e.g. `compositions/azure/resourcegroup/`). Deliberately a **top-level**
   directory, not under `clusters/dev/`, so other clusters can add their own
   pointer Kustomization at `path: ./compositions` and share the exact same
   compositions. Applied by the dependent `crossplane-compositions`
   Kustomization (`dependsOn: flux-system` only — applying a
   Composition/XRD doesn't require its `functionRef` target to already
   exist; Crossplane only resolves that at XR-reconcile time, not apply
   time).
   `teams/` — team resources, one subfolder per team (e.g.
   `teams/mvpdagen/`), with no prefix or suffix on the team name. Each team
   folder owns a `kustomization.yaml` and may contain any layout it needs;
   `app/` and `infra/` folders are optional. A team Kustomization can include
   the shared `teams/_base/` Namespace template and set `namespace: <team>`;
   Kustomize then creates the Namespace and applies that namespace to the
   team's namespaced resources. The current `teams/kustomization.yaml`
   explicitly lists each team because native Kustomize and Flux do not infer
   resources or Namespace names from directory names. Applied by the
   dependent `teams` Kustomization
   (`clusters/dev/teams-kustomization.yaml`, `path: ./teams`), which
   `dependsOn` **both** `flux-system` and `crossplane-compositions`.

### Composition XRD scope must match the composed resource's scope

Crossplane v2 (`v2.4.0` here) requires `mode: Pipeline` Compositions (no
plain `resources:` list) and a `CompositeResourceDefinition`'s `spec.scope`
(`apiextensions.crossplane.io/v2`) must match the scope of whatever it
composes: a `Cluster`-scoped XRD to compose `Cluster`-scoped managed
resources, or `Namespaced` only when every composed resource is namespaced.

**Namespace scope is the repository default.** Keep XRDs and composed
resources namespaced unless the provider schema explicitly requires cluster
scope. The Upbound Azure Network provider resources used here are namespaced,
including `Manager` and `ManagerIpamPool`, so their XRDs must use
`spec.scope: Namespaced` and team Kustomizations may set their namespace.
Verify the installed CRD's `.spec.scope` and `.spec.names.kind` before adding
or changing a composed resource; Azure's resource hierarchy does not by
itself determine Kubernetes CRD scope.

Do not confuse Kubernetes scope with an Azure provider field named `scope`.
For example, `Manager.spec.forProvider.scope.subscriptionIds` selects the
Azure subscription while the Kubernetes resource remains namespaced. A
subscription ID is non-secret configuration; Azure client credentials remain
in the bootstrap-created Secret and must never be committed to manifests.

  This repository uses the namespaced Upbound Azure `ResourceGroup` at
  `azure.m.upbound.io/v1beta1`, so the `XResourceGroup` XRD is namespaced.
  Mismatching
this fails silently at apply time and only errors at XR-reconcile time
(`cannot apply cluster scoped composed resource ... for a namespaced
composite resource`). `scope` is also immutable once the XRD is established
— changing it means deleting and recreating the XRD (and, per
[crossplane#6736](https://github.com/crossplane/crossplane/issues/6736),
restarting the `crossplane` core deployment so its controller picks up the
new scope).

### CRD-dependency deadlock (real bug, already fixed once — don't reintroduce)

Flux's kustomize-controller auto-generates an implicit `kustomization.yaml`
(recursively including *every* YAML under a `path`) when one isn't present.
A resource whose CRD isn't established yet fails its dry-run and **aborts
the entire apply batch for that Kustomization** — including unrelated
resources in the same batch that would have installed the CRD. This
repo hit this twice in production: once with `ProviderConfig` swept into the
root batch, and again with the `Provider` package swept into the same batch
as the `HelmRelease` that provides its CRD — each time silently blocking
*everything* downstream (see git history for the two fix commits).

**When adding a new Crossplane provider/config (or anything else with a
CRD-establishing dependency)**: give its directory its own
`kustomization.yaml`, add a dedicated `dependsOn`-gated Kustomization CR with
a short `retryInterval`/`timeout` (templates in `flux-kustomizations/`:
`azure-provider-kustomization.yaml` and
`azure-provider-config-kustomization.yaml`), and make sure the resource is
referenced by *only* that dedicated Kustomization — never by a parent
directory's `kustomization.yaml` or Flux's implicit recursive scan.

## `infrastructure/` scripts and ordering

`bootstrap.sh` and `teardown.sh` are the only imperative pieces, and their
step order is deliberate:

- **`bootstrap.sh`**: preflight-checks all required CLIs (`gh`, `kiac`,
  `flux`, `kubectl`, `az`, `jq`) up front and fails fast → `kiac create
  cluster --config infrastructure/config.yaml` → idempotently pre-creates the
  `crossplane-system` namespace → creates (or reuses) the
  `azure-crossplane-demo` Azure service principal (override the name via
  `SP_NAME`) and applies it as the `azure-secret` Secret → **then**
  `flux bootstrap`. Azure credentials must exist *before* Flux is
  bootstrapped so the `ProviderConfig` above has working credentials
  immediately instead of erroring out until a human intervenes.
- Credentials are written to `infrastructure/azure-credentials.json`
  (gitignored, chmod 600) and **intentionally never deleted** — re-running
  `bootstrap.sh` reuses the file instead of minting a new service principal.
  If you touch this logic, preserve reuse-if-present semantics.
- **`teardown.sh`** reverses it: deletes the Azure SP by reading `clientId`
  out of `azure-credentials.json` (only removing the file after confirmed
  deletion), then deletes the kiac cluster using the name parsed out of
  `infrastructure/config.yaml` (not hardcoded).
- Both scripts degrade gracefully (warn + skip, don't hard-fail) when an
  optional piece is already missing/absent — keep that behavior when editing.
- Cluster name, addons, and node sizing live in `infrastructure/config.yaml`,
  consumed by `kiac create cluster --config`; `teardown.sh` parses `name:`
  from the same file, so keep both in sync if you change the format.

## Conventions

- Always use conventional commits. In both commits and pull request titles.
- Text must be brief and to the point. Avoid overly verbose prose.
- Fixed, reused names: kiac cluster `dev`, service principal
  `azure-crossplane-demo` (overridable via `SP_NAME`), secret
  `azure-secret`/key `creds`. Changing one requires updating every reference
  (scripts, `provider-config.yaml`, README).
- `bootstrap.sh`/`teardown.sh` use `set -euo pipefail` and idempotent
  `kubectl ... --dry-run=client -o yaml | kubectl apply -f -` for anything
  that may already exist — follow this pattern for new imperative steps
  rather than plain `kubectl create`.
- Flux `Kustomization`s that depend on CRDs from another package use
  `dependsOn` + a short `retryInterval`/`timeout` rather than manual
  ordering, and their target directory must have its own scoped
  `kustomization.yaml` — see the Azure templates in
  `crossplane/flux-kustomizations/`.
