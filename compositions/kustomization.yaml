apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared across clusters: any cluster wanting these compositions adds its own
# dependsOn-gated Kustomization pointing at this directory (see
# clusters/dev/crossplane/flux-kustomizations/compositions-kustomization.yaml)
# - this directory itself is intentionally cluster-agnostic. Add one resources:
# entry per cloud provider subfolder.
resources:
  - azure
